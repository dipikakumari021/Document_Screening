# MongoDB Atlas connection string
# Replace with your own MongoDB Atlas connection URI or local MongoDB instance
DATABASE_URL="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/doc_screening?retryWrites=true&w=majority"

# JWT Secret used for signing session cookies
JWT_SECRET="your_jwt_secret_key_here"
AI_SERVICE_URL="http://localhost:8000"
